{
  "name": "order-portal",
  "version": "1.0.0",
  "description": "Carolina Rolling Order Portal",
  "scripts": {
    "start": "cd backend && npm install && node server.js",
    "heroku-postbuild": "cd frontend && npm install && npm run build"
  },
  "engines": {
    "node": "18.x",
    "npm": "9.x"
  }
}
